<h1><%= @election.name %> の結果</h1>

<table class="table">
  <thead>
    <tr>
      <th>候補者</th>
      <th>所属政党</th>
      <th>獲得票数</th>
    </tr>
  </thead>
  <tbody>
    <% @candidates.each do |candidate| %>
      <tr>
        <td><%= candidate.name %></td>
        <td><%= candidate.party %></td>
        <td><%= candidate.votes.count %> 票</td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "選挙一覧に戻る", admin_elections_path, class: "btn btn-secondary" %>
